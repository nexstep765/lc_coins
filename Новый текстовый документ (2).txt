import random
import pygame

pygame.init()
window = pygame.display.set_mode((700, 500))
pygame.display.set_caption("Coin pvp")


lvl = int(input'lvl?')
lvl2 = int(input'lvl2?')
lvl1 = int(input('Уровень игрока 1? '))
lvl2 = int(input('Уровень игрока 2? '))
sp = 0
sp2 = 0
hp1 = 100 + lvl1
dmg1 = 10 + lvl1 / 10
hp2 = 100 + lvl2
dmg2 = 10 + lvl2 / 10

turn = 0

coin_amount = randint(1, 5)

coin =  randint(0, 1)
coin2 = randint(0, 1)


coin_power = lvl + кол-во монет равных 1

в столкновении побеждает тот у кого больше coin_power, одна монетка ломается при проигрыше, когда монеты кончаются то игрок наносит урон другому. После ломание одной из монет, то монеты обоих игроков заново получают значение.

sp может достигать 45 и -45, sp дает шанс в кол-ве своего значение на выпадение 1 в монете

когда кто-то атаковал, то начинается другой ход, в котором заново появляются монеты и их значение.

слева на экране написаны характеристики игрока1 - lvl, hp, шарики (монеты) над которыми написано 0 либо 1. На правой части также но для игрока 2.
В середине экрана сверху написан номер хода.

Для столкновение нужно каждый раз нажимать пробел.